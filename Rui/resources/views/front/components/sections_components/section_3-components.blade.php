
    @if(!empty($title))
    <p class="info_container__date">
        {{$title}}
    </p>
    @endif
    @if(!empty($date))
        <p class="info_container__date">
            {{$date}}
        </p>
    @endif
