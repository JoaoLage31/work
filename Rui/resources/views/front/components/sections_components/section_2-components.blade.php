<div class="individual_skill">
    @if(!empty($field_date_1))
    <p class="info_container__date">
        {{$field_date_1}}
    </p>
    @endif
    @if(!empty($field_title_1))
        <p class="info_container__title">
            {{$field_title_1}}
        </p>
    @endif
    @if(!empty($field_description_1))
        <p class="info_container__description">
            {{$field_description_1}}
        </p>
    @endif
</div>
